# Foundation — Module 1
pydantic-settings>=2.7.0,<3
pydantic>=2.0.0,<3
python-dotenv>=1.0.0,<2

# UI — Module 2
streamlit>=1.41.0,<2

# Extraction — Module 4 / 5
PyMuPDF>=1.25.0,<2
pytesseract>=0.3.10,<1
Pillow>=10.0.0,<13
pdf2image>=1.17.0,<2
opencv-python>=4.8.0,<5

# Transcription — Module 6 (CPU-optimised faster-whisper)
faster-whisper>=1.1.0,<2

# AI Inference — Module 8 (Ollama HTTP client)
httpx>=0.27.0,<1

# Database — Module 10
sqlalchemy>=2.0.0,<3

# Preprocessing — requires numpy for advanced deskew
numpy>=1.24.0,<3

# Optional dependencies (install as needed):
#   openpyxl               — Excel (.xlsx) export support
#   psutil                 — enhanced system monitoring (CPU/RAM/Disk)


